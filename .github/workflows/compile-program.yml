name: Compile Golang program for multiarch

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: linux
        arch: ['amd64', 'arm']

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '1.20.2'
      - run: |
        GOOS=${{ matrix.os }} \
          GOARCH=${{ matrix.arch }} \
          go build \
            -o ./program/hello-${{ matrix.os }}-${{ matrix.arch }} \
            ./program/hello.go
      - run: ./program/hello